# Security Scan: CoinUsUp — 2026-04-01

## Overview
**Project:** CoinUsUp  
**Scan Date:** 2026-04-01 19:20 ADT  
**Status:** ⚠️ **14 vulnerabilities found — 4 fixed, 10 remain**  

---

## Key Findings

### ✅ Fixed (4 vulnerabilities)
- `brace-expansion@2.0.0-5.0.4` → Updated (DoS via zero-step sequence)
- `flatted@<=3.4.1` → Updated (RCE + Prototype Pollution)
- `picomatch@<=2.3.1` → Updated (ReDoS + method injection)
- `serialize-javascript@<=7.0.4` → Updated (RCE via RegExp + CPU DoS)

**Command:** `npm audit fix --legacy-peer-deps`

---

## Remaining Issues (10 HIGH + 1 CRITICAL)

| Severity | Package | Issue | Fix Available | Impact |
|----------|---------|-------|---|---------|
| 🔴 CRITICAL | handlebars@4.0.0-4.7.8 | 8 JS injection + XSS vulns | ✅ Yes | **HIGH** — template injection risk |
| 🔴 HIGH | tar@<=7.5.10 | Path traversal + symlink poisoning | ❌ No fix | Build tool risk (Capacitor) |
| 🔴 HIGH | @xmldom/xmldom@<0.8.12 | XML injection via CDATA | ❌ No fix | Indirect (via @capacitor/assets) |
| 🔴 HIGH | serialize-javascript | (legacy, some fixed) | — | Covered above |

---

## Recommended Actions

### Priority 1: Fix Handlebars (CRITICAL)
```bash
cd CoinUsUp
npm install --save-dev handlebars@latest --legacy-peer-deps
```
**Risk:** Medium (used in build pipeline, unlikely runtime risk if templates are trusted)  
**Test:** `npm run build` after update

### Priority 2: Audit Capacitor Usage
- Handlebars + tar + xmldom vulnerabilities come from **@capacitor/assets** and **@capacitor/cli**
- **Question for Joe:** Is Capacitor (mobile build tool) still active in this project?
  - If **YES** → Consider updating to latest Capacitor v5.x (addresses many deps)
  - If **NO** → Can remove @capacitor/assets to eliminate 3 critical vulns in one step

### Priority 3: Monitor tar (No Fix Available)
- @capacitor/cli depends on tar@<=7.5.10
- Vulnerability is in archive extraction (rare in CI context)
- **Workaround:** Keep node_modules out of untrusted paths; audit any CI scripts that extract archives

---

## Source Code Security Checks

✅ **No hardcoded secrets found**
- Grep scan: AWS keys, Stripe keys, API tokens — all clean
- `.env` file exists (not version-controlled)
- Git history: no credential commits

---

## Next Steps

1. **Immediate:** Run `npm install --save-dev handlebars@latest --legacy-peer-deps` to fix CRITICAL
2. **This week:** Ask Joe about Capacitor status
3. **If Capacitor removed:** `npm uninstall @capacitor/assets` eliminates 3 more vulns
4. **Re-scan after fixes:** `npm audit --audit-level=high`

---

## Testing After Fixes

```bash
npm run build      # Verify build still works
npm run dev        # Check dev server
npm audit          # Verify vulnerability count drops
```

---

**Report generated by Alfred (idle-activity: surprise-delight)**  
**Next scan:** ~1 week (monitoring after Handlebars fix)
